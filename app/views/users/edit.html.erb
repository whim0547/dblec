<% provide(:title, "Edit user") %>
<% provide(:button_text, 'Save changes') %>
<% provide(:is_new, false) %>

<h1>Update your profile</h1>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
